Composed mini-systems:

- `relay_control` — migrated serial relay POC (active-low GPIO)
- `sensor_telemetry` — analog sample to JSON
- `cellular_data_path` — Quectel bring-up state machine + watchdog
- `command_and_control` — shared command parser for serial and MQTT payloads
