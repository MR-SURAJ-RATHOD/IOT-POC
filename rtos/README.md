# FreeRTOS helpers

Portable pieces (`backoff`, `fsm`) compile in native tests. On-device examples under `examples/rtos/` show tasks, queues, mutexes, semaphores, event groups, timers, and the task watchdog.
