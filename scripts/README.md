# Helper scripts

| Script | Purpose |
| --- | --- |
| [`setup-local-config.ps1`](setup-local-config.ps1) / [`.sh`](setup-local-config.sh) | Copy all `config.example.*` → gitignored local config files |
| [`smoke-verify.ps1`](smoke-verify.ps1) / [`.sh`](smoke-verify.sh) | Build ESP32, STM32, A7672 + Pi dry-run (+ native tests) |

Run from **repo root**. Full instructions: [docs/RUN.md](../docs/RUN.md).
